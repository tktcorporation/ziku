# 公開から3日未満のパッケージバージョンはインストール対象から除外する。
# 新バージョン公開直後のサプライチェーン攻撃（悪意ある publish）を緩和するため。
# ref: https://docs.npmjs.com/cli/v11/commands/npm-install#min-release-age
# 値は日数（整数）。npm が内部で before 日付に変換する。
min-release-age=3
