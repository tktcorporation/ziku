---
"ziku": patch
---

`ziku push` のファイル選択画面を、端末サイズを超えるファイル数・全角文字に対応させる。

ファイルリストはカーソルを中心としたスクロール窓で描画し、窓の外にある件数を `… N more above` / `… N more below` で示す。diff プレビューとリストへ配分する行数は、画面全体が端末行数に収まるように決める。全選択・全解除は窓の外の項目にも効く。

行の切り詰めは端末のカラム数で判定する。全角文字を 2 カラム、結合文字を 0 カラムとして数え、書記素クラスタ境界で切るためサロゲートペアが割れない。
