---
"ziku": patch
---

`ziku push` / `ziku setup --remote` が、PR の head に使うリポジトリが対象の fork であることを確かめるようにする。

認証ユーザーが同名の無関係なリポジトリを持っていると、これまではそこへ同期ブランチを作りにいき、共通の履歴が無い PR として GitHub に拒まれていた。ユーザーには GitHub のエラーがそのまま出るだけで原因が分からなかったため、そのリポジトリ名を挙げて改名か削除を促す失敗として報告する。fork の fork でも、根が対象リポジトリなら従来どおり使う。
