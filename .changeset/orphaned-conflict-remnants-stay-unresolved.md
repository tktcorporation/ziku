---
"ziku": patch
---

開始マーカーを消したコンフリクトの残骸も未解決として扱う。`|||||||` または `=======` で始まり `>>>>>>>` で終わる並びが残っていると `pull --continue` が確定を拒む。単独で現れる `=======`（setext 見出しの下線）・`|||||||`（空セルだけのテーブル行）・`>>>>>>>`（深くネストした引用）は本文として扱う。
