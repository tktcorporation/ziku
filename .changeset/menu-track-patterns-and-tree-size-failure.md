---
"ziku": patch
---

対話メニューから `track` を選べるようにする。メニューは追加するパターンを尋ねてから `track` を実行する。他のコマンドは位置引数がプロジェクトディレクトリだけなので、引数なしのままカレントディレクトリを対象に起動する。

同期対象が多すぎて GitHub がリポジトリのツリーを返し切れなかった `push` の失敗を、ユーザー向けの案内として表示する。スタックトレースとバグ報告の誘導ではなく、ファイル数を減らす手順を示す。
