---
"ziku": patch
---

`ziku setup --remote` が、既定ブランチを引けなかった理由に応じた案内を出すようにする。

トークンを拒否された場合は、トークンの更新を促す認証エラーとして報告する。これまでは理由を問わず「既定ブランチを決められない」として報告し、`.ziku/lock.json` の `source.ref` でブランチを指定するよう案内していたが、`setup` の対象リポジトリにその lock ファイルは存在しないため実行できない案内だった。
