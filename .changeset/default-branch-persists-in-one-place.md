---
"ziku": patch
---

控え直した既定ブランチを書き出す判断を、コマンドごとの return 地点からコンテキスト読み込みの 1 箇所へ移す。パターン未設定で戻る `pull` と、PR を作って戻る `push` では控えがディスクへ残らず、次に GitHub へ問い合わせられないときの取得先が存在しないブランチを指していた。`--dry-run` と読み取り専用のコマンド（`diff` / `status`）は lock を書き変えない。
