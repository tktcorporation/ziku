---
"ziku": patch
---

`ziku push` が、送ると示した削除を実行できないときに失敗として報告するようにする。

削除対象が PR の宛先ブランチに無い場合、これまではそのファイルだけを黙って飛ばし、削除を含まない PR が成功として表示されていた。ziku は同期ベースから削除を導くので、上流で先に消えたファイルがこの形になる。対象のパスを挙げ、`ziku pull` でベースを取り直してから push し直すよう案内する。
