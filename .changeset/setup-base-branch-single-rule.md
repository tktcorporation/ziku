---
"ziku": patch
---

`ziku setup` が PR を向ける既定ブランチを、他のコマンドと同じ規則で決める。規則にケースが増えたとき、setup だけが取り残されることがなくなる。

走査範囲の集約を通らずに gitignore だけでファイルを絞る入口を無くした。その入口は `.ziku/ziku.jsonc` を同期対象から落とすため、使うとテンプレート側のパターン追加がプロジェクトへ届かなくなる。
