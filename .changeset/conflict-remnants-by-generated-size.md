---
"ziku": patch
---

未解決のコンフリクトを、ziku が実際に書き込んだマーカーの長さ以上のラベル付きマーカーが残っているかで判定する。コンフリクトマーカーの書き方を説明する文書を同期していると、本文に書かれた `<<<<<<< LOCAL` を残骸と数えてしまい、解決を終えても `ziku pull --continue` が通らなくなっていた。生成したマーカーの長さは `.ziku/lock.json` に記録し、再開時の走査へ渡す。
