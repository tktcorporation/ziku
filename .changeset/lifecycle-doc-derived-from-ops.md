---
"ziku": patch
---

ライフサイクルドキュメントの関係図とファイル別表を、各コマンドが宣言する `ops` から導出する。

図と表を文字列リテラルで持っていたため、`ops` に宣言された操作がドキュメントへ現れなかった。`status` の `.ziku/ziku.jsonc` 読み取り、`push` の `.ziku/lock.json` 更新、`push` がテンプレートの `README.md` を書き換えるエッジが、いずれも欠けていた。`ops` から集計するようになり、コマンドの操作を足すとドキュメントも同じ差分で変わる。
