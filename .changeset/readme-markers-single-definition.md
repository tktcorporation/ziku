---
"ziku": patch
---

README のマーカー定義と差し替え処理を 1 箇所に集約する。

`FEATURES` / `COMMANDS` / `FILES` のマーカー名と差し替え処理が、ziku 本体とドキュメント生成器に別々に書かれていた。片方の名前だけを変えると、もう片方は「マーカーが無い」として無言で何も書かなくなる。

差し替えはマーカーの不在を戻り値で表す。テンプレートの README を組み直す経路は区画を持たない README に触れず、ドキュメント生成器は不在を書き換え漏れとしてエラー終了する。
