---
"ziku": patch
---

既定で送信対象に入るかの判定を 1 本にまとめる。

非対話実行の既定集合、対話一覧の初期チェック、`ziku.jsonc` の自動同梱を止めるゲートが、同じ `isDefaultSelected` を通る。未解決の衝突・`--include-deletions` でない削除・テンプレート側の削除を取り消すファイルを外すという規則が 1 箇所にしか無くなる。

選択画面へ渡す印は 3 つとも必須にした。渡し忘れは空集合として扱われ、テンプレートの削除を取り消すファイルが対話実行でだけ既定チェック済みになる。
