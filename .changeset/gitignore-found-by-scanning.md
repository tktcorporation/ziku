---
"ziku": patch
---

同期対象のサブツリーに実在する `.gitignore` を走査して読む。`.claude/sub/.gitignore` のように include パターンの先頭セグメントより深い位置に置かれた規則も判定に効くようになり、そこで無視されているマシン固有の設定や資格情報が pull で上書きされたりテンプレートへ送られたりしなくなる。走査は include パターンが到達しうる範囲に限り、`node_modules` と `.git` には入らない。
